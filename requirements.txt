# ML conference papers pipeline — core (fetch, merge, vocabulary/title keywords, analysis)
pandas>=2.0,<3
openpyxl>=3.1
matplotlib>=3.7
seaborn>=0.13
requests>=2.28
beautifulsoup4>=4.12

# OpenReview client (used by fetch_icml_papers.py)
openreview-py>=2.0

# --- Optional: KeyBERT title keywords (installs PyTorch; large download) ---
# keybert>=0.8
# sentence-transformers>=2.2

# --- Optional: word clouds in topic_analysis.py ---
# wordcloud>=1.9
